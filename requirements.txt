# Core (always needed)
sentence-transformers>=2.2.0
faiss-cpu>=1.7.0
numpy>=1.24.0
python-dotenv>=1.0.0
soundfile>=0.12.0

# Offline ASR — MLX Whisper (Apple Silicon)
mlx-whisper>=0.1.0
sounddevice>=0.4.0
scipy>=1.10.0

# Offline LLM — Qwen2.5-1.5B via MLX (Apple Silicon)
mlx-lm>=0.10.0

# Offline TTS — AI4Bharat Indic-Parler-TTS
parler-tts>=1.0.0
transformers>=4.40.0

# Telegram Bot
python-telegram-bot[webhooks]>=20.7

# MCP Server
mcp[cli]>=1.0.0

# Demo UI (optional)
# gradio>=4.0.0

# Dev
pytest>=7.0
ruff>=0.3.0
